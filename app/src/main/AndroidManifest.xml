<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kronologia.spsport">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="com.kronologia.spsport.AppController"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainMenuActivity">


            Vrai activité de lancement, supprimer l'autre intent filter qui est là pour les tests
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".ChoixMissionActivity" />
        <activity android:name=".ParametresSessionActivity">
          <!-- <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter> -->
        </activity>
        <activity
            android:name=".CircuitTrainingActivity"
            android:label="@string/title_activity_circuit_training"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".EntrainementActivity"></activity>
    </application>

</manifest>